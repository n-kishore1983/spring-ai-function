spring:
  application:
    name: spring-ai-function
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o
          temperature: 0.7
          max-tokens: 1000

ninjas:
  api:
    key: ${NINJAS_API_KEY}
